/// @function scr_toggle()
/// @description Toggles a boolean value
/// @param value | value to be toggled
// Returns bool

var value = argument0;
//start
    {
    if (value) return(false);
    else return(true);
    }
